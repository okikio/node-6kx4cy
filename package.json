{
  "name": "node-starter",
  "version": "0.0.0",
  "scripts": {
    "start": "esbuild --bundle index.js --outfile=out.js --format=esm && gzip-size out.js && ls -al",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@chakra-ui/react": "^2.5.1",
    "@emotion/react": "^11.10.6",
    "@emotion/styled": "^11.10.6",
    "date-fns": "^2.30.0",
    "framer-motion": "^10.2.4",
    "gzip-cli": "^1.2.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.17.10",
    "esbuild-wasm": "^0.17.19",
    "gzip-size-cli": "^5.1.0",
    "wc": "^0.0.1"
  }
}
